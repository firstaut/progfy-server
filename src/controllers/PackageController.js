const Package = require('../models/Package');

const addPackage = async (req, res) => {
    try {
        
    } catch (error) {
        console.log(error);
    }
}

const subtractHours = async (req, res) => {

    try {
     
        const {
            customerId,
            hours
        } = req;

        

    } catch (error) {
        console.log(error);
    }
}




module.exports = {
    addPackage,
    subtractHours,
}